#include<stdio.h>
int main()
{
	int c=0,i,a;
	printf("enter the number:");
	scanf("%d",&a);
	for(i=1;i<=a;i++)
	{
		if(a%i==0)
		{
			c++;
		}
	}
	if(c==2)
	{
		printf("is a prime ");
	}
	else 
	{
		printf("not a prime");
	}
	return 0;
}
